# P1-D 发布工程实施提示

本阶段只处理 CI、质量门禁、依赖锁定、可重复容器构建、不可变部署、环境校验、显式数据库迁移、发布和回滚 Runbook。

硬性边界：不连接或部署生产、不读取真实 Secret、不改变业务、支付、库存、物流和测算逻辑、不删除旧部署脚本、不提交或推送。新流程默认安全，生产只接受摘要镜像，迁移必须先备份并在隔离副本演练，任何失败都阻止发布。完成后仅可进入最终上线审计，不得直接宣布 GO。
