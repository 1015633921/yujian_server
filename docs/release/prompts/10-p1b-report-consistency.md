# P1-B 报告一致性实施提示

本阶段负责报告版本、不可变快照、测算依据一致性、地点校准准确性，以及海报与推荐方案的数据版本一致性。实施边界、完成条件和最终输出以本任务原始 P1-B 指令为准。

硬性门禁：不连接生产、不部署、不调用外部服务、不编造坐标、不修改测算算法；所有私有报告接口按 current user 授权；迁移 additive 且可回退；`REPORT_VERSIONING_V2_ENABLED` 默认关闭；缺少可靠全国地点数据或 MySQL 门禁时必须标记 BLOCKED；项目整体始终 NO-GO。
