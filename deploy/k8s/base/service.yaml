apiVersion: v1
kind: Service
metadata:
  name: yujian-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: yujian-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
